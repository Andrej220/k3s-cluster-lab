apiVersion: v1
kind: Service
metadata:
  name: petstore-service
  namespace: petstore
spec:
  type: NodePort  
  selector:
    app.kubernetes.io/name: swagger-petstore
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080  
    nodePort: 30080   # port between 30000-32767
